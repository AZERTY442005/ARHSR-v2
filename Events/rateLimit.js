// ©2022 AZERTY. All rights Reserved | AZERTY#9999

module.exports = {
    name: 'rateLimit',
    async execute(info, bot, config) {
        // console.log(info)
        // console.warn(`Rate limit hit ${info.timeDifference ? info.timeDifference : info.timeout ? info.timeout: 'Unknown timeout '}`)
    }
}